#import "/templates/blog.typ": *
#import "/templates/enums.typ": *

// @generated by scripts/i18n/translate.mjs
// prompt_version: wonderland-i18n-v1
// model: gpt-4o-mini
// source: content/article/en/tutorials/2017/migrating-ghost-to-1-0-on-centos7.typ
// source_sha256: sha256:f60be9ab5b46c869a23fec8db3ebf3bf5735329e23a2f039147c67b8c1d69f65

#show: main.with(
  title: "从 0.11.x 迁移 Ghost 到 1.0 在 CentOS 7",
  desc: [从 0.11.x 迁移 Ghost 到 1.0 在 CentOS 7],
  date: "2017-07-29",
  tags: (

    blog-tags.ghost,
    blog-tags.linux,
  
  ),
  lang: "zh",
  i18nKey: "tutorials/2017/migrating-ghost-to-1-0-on-centos7",
  sourceLang: "en",
  translationSourceHash: "sha256:f60be9ab5b46c869a23fec8db3ebf3bf5735329e23a2f039147c67b8c1d69f65",
  translationStatus: "machine",
)

Ghost 已经发布了 1.0，带来了重大变化，因此没有平滑的升级方法，只能进行全新安装。以下是我将这个博客迁移到 Ghost 1.0 的过程。

已经有 #link("https://docs.ghost.org/docs/migrating-to-ghost-1-0-0")[官方教程] 来帮助你完成这个过程，但由于它 *仅官方支持 Ubuntu*，所以我必须在我的 `CentOS 7` 服务器上探索自己的方法。事实证明，与 Ubuntu 版本相比，修改并不多，因此我只是将其记录下来作为我的迁移记录。

= 备份

Ghost 的新版本使用几乎相同的数据结构，因此请按照以下步骤备份你的内容：
- 转到你的 `Admin` 部分，在 `Labs` 下，点击 `Export` 以制作你的帖子、自定义代码注入等的备份 json 文件。
- 转到你服务器上的网站目录，复制 `content` 文件夹，你的图片就存放在那里。
- 备份你的 `themes` 文件夹，以防你在其中放置了自定义文件，例如我自己的 `highlightjs`。

= 安装

在 Ghost 1.0 或更高版本中，它使用自己的 CLI，称为 `ghost` 来管理网站，因此通过 npm 安装它：

```bash
sudo npm i -g ghost-cli
```

然后创建你的网站文件夹（我自己是在 `/usr/share/nginx/` 下创建的）：

```bash
sudo mkdir /usr/share/nginx/ghost
cd /usr/share/nginx/ghost
```

然后开始安装过程：

```bash
ghost install local
```

在这里，我使用 `ghost install local` 而不是 `ghost install` 来绕过 mysql 配置。

当安装成功完成后，它会立即运行。如果你之前使用 `nginx/Apache` 运行 Ghost 的旧版本，你可以直接从浏览器访问这个新网站。

然而，它将你的 `site url` 默认配置为 `http://localhost:2368`，你需要手动更改它，以及其他配置，如 `mail`。在你的网站根文件夹下：

```bash
vim config.development.json
```

这使得你的网站在开发模式下运行，你可以直接将其复制到 `config.production.json` 以使你的网站在生产模式下运行。

在修改完配置文件后，运行：

```bash
ghost restart
```

你的网站将使用新配置重新启动，准备就绪！
